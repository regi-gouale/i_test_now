name: I-Test-Now

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build Web
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: subosito/flutter-action@v1
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
      - run: |
          cd build/web
          git init
          git config --global user.name "Régi GOUALE"
          git config --global user.email "regi.gouale@gmail.com"
          git status
          git remote add origin 
          git add --all
          git commit -m "Deploy to GitHub Pages"
          git push origin gh-pages --force